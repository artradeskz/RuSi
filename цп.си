цел запуск() {
    цел i = 0;
    
    sys_write(1, "=== Тест цикла цп ===\n", strlen("=== Тест цикла цп ===\n"));
    
    цп (i < 5) {
        sys_write(1, "i = ", strlen("i = "));
        print_int(i);
        sys_write(1, "\n", 1);
        i = i + 1;
    }
    
    sys_write(1, "=== Цикл завершён ===\n", strlen("=== Цикл завершён ===\n"));
    sys_write(1, "Финальное i = ", strlen("Финальное i = "));
    print_int(i);
    sys_write(1, "\n", 1);
    
    вернуть i;
}
